{
  "name": "totalanime",
  "version": "2.4.1",
  "private": true,
  "description": "TotalAnime 2.0 - Modern Anime Streaming Platform",
  "scripts": {
    "db:reset": "supabase db reset",
    "db:test": "supabase test db",
    "migrate:users": "tsx scripts/migrate-users.ts",
    "migrate:users:dry": "tsx scripts/migrate-users.ts --dry-run",
    "migrate:data": "tsx scripts/migrate-data.ts",
    "migrate:data:dry": "tsx scripts/migrate-data.ts --dry-run",
    "set:dev-passwords": "tsx scripts/set-dev-passwords.ts",
    "api:dev": "npm --prefix api run dev",
    "api:build": "npm --prefix api run build",
    "api:test": "npm --prefix api run test",
    "api:worker": "npm --prefix api run worker:dev",
    "admin:dev": "npm --prefix admin run dev",
    "admin:build": "npm --prefix admin run build",
    "admin:lint": "npm --prefix admin run lint",
    "admin:test": "npm --prefix admin run test",
    "web:dev": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "web:lint": "npm --prefix web run lint",
    "web:test": "npm --prefix web run test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "dotenv": "^16.4.7",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.9",
    "@types/pg": "^8.23.1",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
